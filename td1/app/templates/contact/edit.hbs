 <h4>Modifier un contact</h4>
                    <hr />
                    <form>
                        <div class="form-group">
                            <label for="FirstName">Prénom</label>
                            <Input type="text" class="form-control" @value={{model.nom}}/>
                        </div>
                        <div class="form-group">
                            <label for="LastName">Nom</label>
                            <Input type="text" class="form-control" @value={{model.prenom}}/>
                        </div>
                        <div class="form-group">
                            <label for="email">Email </label>
                            <Input type="email" class="form-control" @value={{model.email}}/>
                        </div>
                        <LinkTo class="btn btn-primary" @route="contact.index">Valider</LinkTo>
                    </form>
{{outlet}}